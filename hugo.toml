baseURL = "https://thedexs.com/"
defaultContentLanguage = "en"
theme = "stack"
enableRobotsTXT = true

# 全局通用配置
[params]
    mainSections = ["post", "posts"]
    favicon = "/favicon.png"

    [params.dateFormat]
        published = "2006-01-02"
        updated = "2006-01-02"

    [params.widgets]
        homepage = [
            { type = "search" },
            { type = "categories" },
            { type = "tag-cloud" }
        ]

# 多语言配置
[languages]

    # 1. 默认语言：英文
    [languages.en]
        languageCode = "en"
        languageName = "English"
        title = "Dex's Blog"
        weight = 1

        [languages.en.params.sidebar]
            compact = false
            avatar = "/avatar.jpg"
            subtitle = "Tech & Thoughts"

        # 英文主导航菜单
        [[languages.en.menu.main]]
            identifier = "home"
            name = "Home"
            url = "/"
            weight = 1
            params = { icon = "home" }

        [[languages.en.menu.main]]
            identifier = "archives"
            name = "Archives"
            url = "/archives/"
            weight = 2
            params = { icon = "archives" }

        [[languages.en.menu.main]]
            identifier = "search"
            name = "Search"
            url = "/search/"
            weight = 3
            params = { icon = "search" }

        [[languages.en.menu.main]]
            identifier = "about"
            name = "AboutMe"
            url = "/about/"
            weight = 4
            params = { icon = "user" }

        # 英文社交菜单
        [[languages.en.menu.social]]
            identifier = "youtube"
            name = "YouTube"
            url = "https://www.youtube.com/@ZeRuiDong"
            params = { icon = "brand-youtube" }

        [[languages.en.menu.social]]
            identifier = "email"
            name = "Email"
            url = "mailto:dex222444@gmail.com"
            params = { icon = "mail" }
   

# 允许 Markdown 内渲染原生 HTML 标签与颜色样式
[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
 # 将 page 目录下的页面 URL 简化（去除 /page/ 前缀）
[permalinks]
    page = "/:slug/"